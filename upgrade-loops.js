const products = ['Camiseta de Pokemon', 'Pantalón coquinero', 'Gorra de gansta', 'Camiseta de Basket', 'Cinrurón de Orión', 'AC/DC Camiseta'];
let newArray = [];

for (let i = 0; i < products.length; i++) {
    newArray = (products[i].includes('camiseta'))
}